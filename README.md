# GHOST RUN — 실제로 움직여 유령과 거리를 벌리세요

기존 SwiftUI·MapKit·CoreLocation 프로젝트를 유지한 iOS 17+ 생존 러닝 MVP입니다. GPS, 회전 가능한 지도, 상태 머신, 10초 준비, Ward/Safe Light, 점수·햅틱·위치 검증을 재사용했습니다.

## 실행과 번들 ID

1. GHOST RUN.xcodeproj → GHOST RUN 스킴을 엽니다.
2. 실제 iPhone에서 Debug로 실행하고 정확한 위치와 앱 사용 중 위치 권한을 허용합니다.
3. 실내는 Debug 가상 이동을 켜고 속도/방향을 조절합니다. 초기 속도는 0입니다.
4. Release에는 아래의 별도 건물 지도 서버 설정이 필요합니다.

앱 이름은 **고스트 런 / GHOST RUN**, 새 번들 ID는 **com.hajaeyun.GhostRun**입니다. 테스트 번들은 com.hajaeyun.GhostRunTests입니다. Xcode 프로젝트·스킴·앱 타깃 표시 이름도 GHOST RUN으로 맞췄습니다. Swift 모듈 이름(ZombieRun)은 기존 테스트 호환성을 위해 유지했습니다.

번들 ID를 변경했으므로 기존 com.hajaeyun.NightSignal 앱과 별도 앱으로 설치됩니다. 이전 앱/기록은 삭제하지 않았으며 자동 이전하지 않습니다. 위치 권한·안전 확인·최고 기록도 새 앱에서 별도로 시작합니다.

## 추격 원칙

유령은 보행로에 묶이지 않습니다.

- 조회한 지도에서 장애물과 겹치지 않는 구간: 바로 접근합니다.
- 건물과 겹치는 구간: 로컬 A* 계산으로 돌아갑니다.
- 지도 없음/불완전 응답/범위 밖/막힌 출발·목적지: 대기하고 재시도합니다.
- 실패했다고 건물을 무시하는 직선 추격으로 바꾸지 않습니다.
- 새 스폰은 막힌 후보 대신 다른 검증된 후보를 선택할 수 있지만, 기존 유령을 다른 출발점으로 순간이동시키지 않습니다.

OpenStreetMap에 등록된 건물·건물 부분·물 영역·벽/울타리를 장애물로 사용합니다. 5m 탐색 격자, 2m 여유 간격과 선분 교차 검사로 계산하고, 경로를 단순화할 때도 전체 대체 선분을 검사합니다. 기존 WalkingPath/WalkingCursor는 검증된 경로를 이동하는 공통 도구로 재사용합니다.

지도 누락/갱신 지연·GPS 오차 때문에 **현실의 모든 건물/장애물을 완벽히 판별한다고 보장하지 않습니다.** 중정/안뜰은 보수적으로 막고, 건물 아래 통로·교량·다층 공간은 정확히 표현하지 않습니다. 실제 교통·사유지·출입 제한을 항상 우선해야 합니다.

## 게임 규칙

- 10초 준비 후 GPS와 건물 지도/추격 경로가 준비되면 RUN입니다.
- 준비 중부터 유령 6마리를 목표로 배치하며, 생존 90초에 10마리, 240초에 최대 14마리로 늘립니다. 사라진 유령도 현재 웨이브 수까지 보충합니다. 경로/지도 검증이 실패하면 목표 수보다 적게 등장할 수 있습니다.
- 검증된 경로 하나에서 최대 2마리를 생성하고 다음 무리는 다른 방향을 탐색합니다. 성공 후 2초, 실패 후 최소 15초 간격입니다. 경로상 스폰 거리 100~250m, 플레이어와 최소 80m, 다른 유령과 최소 24m 간격을 확인합니다.
- 기본 속도 1.35m/s, 점진적인 압박과 회복 기회를 적용하며 최대 2.35m/s입니다.
- 첫 60초는 적응 구간(유령 속도 ×0.8), 이후 이동 60초/회복 30초가 반복됩니다. 회복 구간은 속도 ×0.6이며 새 무리를 보충하지 않습니다. 걷기도 거리·스킬 충전에 같은 기준으로 인정하며 전력질주를 요구하지 않습니다. 운동 처방이나 효과 측정 기능은 아닙니다.
- 장애물이 없는 시야 구간의 거리 또는 검증된 우회 경로 거리를 HUD/위험도에 사용합니다. 벽 건너 가까운 유령을 단순 직선거리로 잡힘 판정하지 않습니다.
- 정확한 최신 위치에서 15m 미만이 3초 연속 유지되어야 잡힙니다. 보호/재탐색/불확실한 위치·지도에서는 잡힘을 보류합니다.
- 실제 이동 100m마다 +50점. 위기를 실제 이동으로 벗어나면 +120점. 생존 초×2, 이동거리×1.2, Safe Light×80을 합산합니다.
- Ward는 유령을 4초 멈춥니다. 초기 1회, 최대 2회입니다.
- 비상 사격: 초기/최대 탄약 3발, 사거리 60m, 재사용 2초입니다. 큰 버튼으로 시야 안 가장 가까운 유령을 자동 조준합니다. 지도에서 발사 선분이 건물/벽과 겹치지 않는지 확인하며, 모르는 지형에는 발사하지 않습니다. 탄환이 0.4초 동안 이동한 뒤 유령이 사라집니다. 유효 위치를 잃거나 일시정지하면 비행 중인 탄환은 취소/환불합니다. 처치 점수·충전 보너스는 없습니다.
- 처치 직후 보충은 최소 20초와 추가 실제 이동 30m 이후 가능합니다. 서서 쏘는 것만으로 무리와 점수를 반복 생성할 수 없습니다.
- 파동: 초기 충전 0%, 검증된 이동 200m로 완충되어 8초간 추격을 멈춥니다. 걷기/러닝의 충전량은 거리당 동일합니다. 시간 경과·처치·보급·GPS 튐으로 충전되지 않습니다. 파동은 유령을 순간이동시키거나 벽 너머로 밀지 않습니다.
- 보급 상자는 기존 Safe Light 모델/도달 검증을 재사용하며 Apple 보행 경로 위 130~220m에 배치합니다. 검증된 이동으로 18m 도달 범위를 만족하면 탄약 +3(최대 3), 12초 보호와 Ward를 얻습니다. 접근 어려운 목표는 일시정지에서 건너뛸 수 있습니다.
- 위험 햅틱과 선택형 한국어 음성 경고를 사용합니다. 위험한 상황에서는 사격/달리기를 시도하기보다 즉시 일시정지하고 주변을 확인하세요.

## 코드 구조

- Game/GameEngine.swift: 기존 GPS·세션 규칙, 목표, 추격 갱신, 잡힘/점수
- Game/GameConfiguration.swift: 준비, 속도, 거리, 경로 갱신, 지도 캐시/격자 설정
- Models/ObstacleMap.swift: 지역 좌표, 건물 윤곽, 공간 색인, 점/선분 충돌 검사
- Services/ObstaclePathPlanner.swift: 장애물 회피 A*와 검증된 경로 단순화
- Services/ObstacleDataService.swift: Overpass 조회·캐시·요청 합치기·응답 검증
- Services/GhostRoutingService.swift: 유령은 건물 회피, Safe Light는 MapKit walking으로 분리
- Models/WalkingPath.swift: 기존 경로 커서 재사용, 장애물 지도 메타데이터 유지
- Services/LocationService.swift: 기존 실제 GPS/Debug 입력 유지
- Views/: 지도·HUD·브리핑·결과·안내
- GhostRunTests/: 지도/시계/위치/HTTP를 주입한 자동 테스트

## 지도 요청·배터리·실패 처리

주변 약 650m 사각 영역을 조회하며 중심을 0.002도 격자로 둥글립니다. 실제 플레이어 좌표나 전체 경로를 Overpass 쿼리에 그대로 넣지 않습니다. 다만 해당 지역과 접속 IP는 서버에 전달됩니다.

조회는 한 번에 하나이고 같은 요청을 합칩니다. 메모리 캐시는 5분, HTTP 요청은 최소 60초 간격, 타임아웃은 최대 30초입니다. 경로 계산은 기기에서 비동기로 수행합니다. 개별 유령 경로는 기본 최소 8초 간격으로, 플레이어 25m 이동·경로 만료/차단 등에 맞춰 갱신합니다. 추격 재탐색은 동시에 최대 2개, 오래 기다린 유령부터 처리합니다. 무리 스폰은 별도 작업 하나로 제한합니다. HTTP를 매 프레임 요청하지 않습니다.

HTTP 오류, 빈 결과, 타임아웃 remark, 누락/연결 불가 윤곽은 열린 공간으로 간주하지 않습니다. 다중 외곽선과 중첩된 building outline을 처리합니다. 조회 실패 시 기다리며 무제한 즉시 재요청하지 않습니다. 앱 종료/일시정지 시 요청을 취소하고 늦은 응답을 버립니다. 세션 종료 시 지도·경로 메모리를 정리합니다.

GPS는 유효 좌표·정확도 25m 이내·최근 10초·순서·측정/계산 속도 8m/s 이하를 검증합니다. 잡힘/목표에는 더 엄격한 기준을 적용합니다. 위치 점프/과속은 일시정지합니다. 배경/잠금 시 게임과 위치 수집을 멈추며 복귀 후 수동 재개합니다. Always/백그라운드 위치 권한은 사용하지 않습니다.

플레이 중에는 이동 거리 필터 없이 위치 갱신을 받아 정지 상태도 추적합니다. 위치의 수신 시각과 실제 거리 계산용 기준점을 구분하므로, 정지 중 속도가 측정되지 않아도 신호 끊김으로 오인하지 않습니다. 최신 유효 위치가 10초 넘게 없으면 전체 화면을 막지 않고 HUD에 연결 회복 상태를 표시합니다. 추격/점수/잡힘을 멈추고 지도 캐시와 위치 수집은 유지합니다. 유효 위치가 돌아오면 자동 재개하며, 끊긴 구간의 거리·아이템은 인정하지 않고 새 기준점으로 시작합니다. 복귀 직후 3초는 잡힘 판정을 유예합니다. 사용자가 누른 일시정지와 백그라운드/과속 일시정지는 자동 재개하지 않습니다.

## 개발용과 출시용 서버

Debug 기본 주소는 https://overpass-api.de/api/interpreter 입니다. 이 연결은 개인 개발·검증용이며, 공개 Overpass 서버를 출시 앱의 상시 백엔드로 사용하지 않습니다.

Release는 공용 서버로 자동 연결하지 않습니다. 소유/계약한 Overpass 호환 HTTPS 서버 주소를 Info.plist의 **GhostObstacleEndpoint** 문자열에 설정해야 합니다. 미설정이면 앱은 이유를 표시하고 게임 시작을 보류합니다. 서버 개설/계약/배포는 이번 작업에서 하지 않았습니다.

- [공개 서버 사용 안내](https://dev.overpass-api.de/overpass-doc/en/preface/commons.html)
- [지도 데이터 출처·ODbL](https://www.openstreetmap.org/copyright)

지도 화면과 앱 정보에 © OpenStreetMap contributors · ODbL 출처 링크를 표시합니다. 실제 출시 전에는 선택한 서버의 로그/보관 정책과 개인정보 신고를 재검토해야 합니다.

## Debug와 검증

Debug 도구: 가상 속도/방향, 유령/회복 지점 생성, 유령 속도·위치 조절, 강제 재탐색, 위험도 표시, 게임 오버. 보라색 점선은 추격 경로, 노란 점선은 회복 경로이며 선택형 주황 윤곽은 건물·장애물입니다. 경로선은 무리로 인한 화면 혼잡을 줄이기 위해 기본 숨김이며 도구 메뉴에서 켭니다. 지도 기본 범위를 넓히고 HUD에 현재 유령 수를 표시합니다. Release에서는 개발용 시각화/가상 조작이 제외됩니다.

Product → Test: 기존 동작과 빈 공간 직행, 건물/모서리/벽 우회, 지도 불량 거부, 다중 윤곽/중첩 관계, HTTP 캐시/실패 대기, 벽 건너 잡힘 방지, 보행 경로를 벗어난 가상 이동을 검증합니다. 추가 검증은 초기 무리/보충/상한/재탐색 동시성, 위치 자동 회복, 사격 비행·시야·탄약 상한·환불·보충 지연, 이동 전용 파동 충전·회복 리듬입니다.

동일 소스로 iPhone Debug 서명 빌드와 unsigned Release 빌드를 통과했습니다. 재윤의 iPhone의 com.hajaeyun.GhostRun 앱을 업데이트 설치했습니다. 시뮬레이터에서 실제 서울 지도 기반으로 유령 6마리와 넓어진 지도/HUD를 확인했습니다. 실제 기기의 야외 GPS 수신/복구와 다중 유령 난이도는 별도 현장 확인이 필요합니다.

서울 공개 좌표의 실제 응답에서 1,695개 장애물 윤곽을 읽고 우회를 확인했습니다. 실제 iPhone의 GPS·지도 정합성, 골목/공원·배터리·체감 난이도는 야외 검증이 필요합니다. 지도 데이터가 없는 지역이나 통로에서 대기가 잦을 수 있습니다. 이 검증은 현실의 통행 안전 보장이 아닙니다.
